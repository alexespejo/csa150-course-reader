---
import Layout from "../../../layouts/Layout.astro";
import Header from "../../../components/Header.astro";
const { useLayout } = Astro.props;
---

<Layout
 title="Week 2"
 dir="w02/"
 displayLayout={useLayout}
 chapterSubSections={[]}
>
 <Header id="E">Declaring &amp; Defining</Header>
 <div>
  <p class="firstLine">
   The functions don't yet exist, but we can declare (or <mark>prototype</mark>)
   them right before the <tt>main</tt> function starts like this:
  </p>
  <pre
   class="language-cpp"><code>void printTitle();
double getInput();
double ouncesToTons(double ouncesPerBox);
double boxesPerTon(double tonsPerBox);
void printResults(double tons, double boxes);</code></pre>

  <h2>Function Definitions</h2>
  <p>
   Once you have done that, make a copy of the prototypes
   <mark>following</mark> the <tt>main</tt> function. Replace each of the semicolons
   with a function body. Then, copy the original code (from your homework solution)
   into the appropriate body, and your program will work.
  </p>
  <p>
   Here's a link to
   <a
    target="_blank"
    href="https://replit.com/@stephengilbert/Decomposition?v=1"
    title="The H00 program broken into functions."
   >
    my version of this program
   </a> if you want to compare it to yours.
  </p>
 </div>
</Layout>
